#text(size: 18pt, weight: "bold")[Symbolverzeichnis] <sec:symbole>

#table(
  columns: (3cm, 1fr),
  inset: 6pt,
  [*Symbol*], [*Bedeutung*],
  [$bb(N)$], [Menge aller natürlichen Zahlen ohne die Null],
  [$bb(N)_0$], [Menge aller natürlichen Zahlen einschließlich Null],
  [$bb(Z)$], [Menge aller ganzen Zahlen],
  [$bb(Q)$], [Menge aller rationalen Zahlen],
  [$bb(R)$], [Menge aller reellen Zahlen],
)
